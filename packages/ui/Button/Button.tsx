import styled from '@emotion/styled';

const Button = styled('button', {
  label: 'AdornButton-root',
})({
  background: 'pink',
  border: 'none',
  padding: '10px',
  borderRadius: '32px',
});

export default Button;
